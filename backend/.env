# Server
PORT=3000

# MySQL Database
DB_HOST=localhost
DB_USER=chathurya
DB_PASSWORD=password
DB_NAME=survivorsync

# JWT Secrets
# Use one secret for access token and one secret for refresh token for better security separation
JWT_ACCESS_SECRET=some_super_secure_random_string
JWT_REFRESH_SECRET=some_other_super_secure_random_string


# Optional: environment mode
NODE_ENV=development
